// Stub for binary model weights consumed via require('*.bin') in
// src/services/moderation/tfjs.service.ts. Tests don't run inference, so
// returning a fake numeric asset id is enough to satisfy expo-asset.
module.exports = 1;
